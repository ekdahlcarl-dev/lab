services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lab
      POSTGRES_USER: lab
      POSTGRES_PASSWORD: lab
    ports:
      - "5432:5432"
    volumes:
      - lab-postgres:/var/lib/postgresql/data

volumes:
  lab-postgres:
